import LeadForm from "@/components/site/lead-form"; import { pageCopy, isLocale } from "@/data/site"; import { notFound } from "next/navigation"; export default async function Page({ params }: PageProps<"/[locale]/aloqa">) { const { locale } = await params; if (!isLocale(locale)) notFound(); const copy = pageCopy.aloqa; return <section className="wrap section page"><p className="kick">TOGO GROUP</p><h1>{copy.title[locale]}</h1><p className="lead">{copy.lead[locale]}</p><LeadForm locale={locale} /></section>; }
