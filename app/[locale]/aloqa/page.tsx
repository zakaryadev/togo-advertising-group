import { ReferenceContactPage } from "@/components/site/reference-pages"; import { isLocale } from "@/data/site"; import { notFound } from "next/navigation"; export default async function Page({ params }: PageProps<"/[locale]/aloqa">) { const { locale } = await params; if (!isLocale(locale)) notFound(); return <ReferenceContactPage locale={locale} />; }
