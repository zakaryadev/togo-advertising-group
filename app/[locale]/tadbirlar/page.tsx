import { ReferenceEventsPage } from "@/components/site/reference-pages"; import { isLocale } from "@/data/site"; import { notFound } from "next/navigation"; export default async function Page({ params }: PageProps<"/[locale]/tadbirlar">) { const { locale } = await params; if (!isLocale(locale)) notFound(); return <ReferenceEventsPage locale={locale} />; }
