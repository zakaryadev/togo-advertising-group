import { StaticPage } from "@/components/site/sections"; import { isLocale } from "@/data/site"; import { notFound } from "next/navigation"; export default async function Page({ params }: PageProps<"/[locale]/tadbirlar">) { const { locale } = await params; if (!isLocale(locale)) notFound(); return <StaticPage locale={locale} page="tadbirlar" />; }
