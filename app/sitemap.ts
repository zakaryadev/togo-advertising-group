import type { MetadataRoute } from "next"; import { locales, portfolioCategories, services, siteUrl } from "@/data/site"; export default function sitemap(): MetadataRoute.Sitemap { const routes = ["", "/xizmatlar", "/portfolio", "/narxlar", "/tadbirlar", "/jarayon", "/rekvizitlar", "/aloqa"]; return locales.flatMap((locale) => [...routes.map((path) => ({ url: `${siteUrl}/${locale}${path}` })), ...services.map((service) => ({ url: `${siteUrl}/${locale}/xizmatlar/${service.slug}` })), ...portfolioCategories.map(([category]) => ({ url: `${siteUrl}/${locale}/portfolio/${category}` }))]); }
